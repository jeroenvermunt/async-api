async def {{ method_name }}(self{% for argument in arguments %}, {{ argument }}{% endfor %}, **kwargs):
    url = self.base_url + '{{ endpoint }}'.format({% for argument in arguments %}{{ argument }}={{ argument }},{% endfor %})
    return await self.{{http_method}}(url, **kwargs)